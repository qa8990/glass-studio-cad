[project]
name = "glass-studio-cad"
version = "0.1.0"
description = "Skeleton del prototipo de plataforma de agentes para diseño de vidrio"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.uv]
package = false
